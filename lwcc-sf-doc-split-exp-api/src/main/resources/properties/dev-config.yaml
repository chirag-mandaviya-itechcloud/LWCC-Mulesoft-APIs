# ============================================
# DEV Environment Configuration
# lwcc-sf-doc-split-exp-api
# ============================================

http:
  port: "8081"

mule:
  env: "dev"

# API Manager Autodiscovery ID
# TODO: Replace with actual API ID from API Manager after registration
api:
  id: "0"
  name: "lwcc-sf-doc-split-exp-api"
  version: "v1"

# System API Connection
# TODO: Replace PLACEHOLDER with actual CloudHub URL after deployment
sysapi:
  host: "lwcc-sf-doc-split-sys-api-dev-PLACEHOLDER.vhj0p8.usa-e1.cloudhub.io"
  port: "443"
